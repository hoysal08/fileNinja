export declare type FileSizeInfo = {
    success: boolean;
    sizeInBytes?: number;
    errorMessage?: string;
};

export declare type LineCountResult = {
    success: boolean;
    lineCount?: number;
    errorMessage?: string;
};

export declare type WordCountResult = {
    success: boolean;
    wordCount?: number;
    errorMessage?: string;
};

export declare type CharacterCountResult = {
    success: boolean;
    characterCount?: number;
    errorMessage?: string;
};