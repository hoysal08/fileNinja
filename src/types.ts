export declare type FileSizeInfo = {
    success: boolean;
    sizeInBytes?: number;
    errorMessage?: string;
};

export declare type LineCountResult = {
    success: boolean;
    lineCount?: number;
    errorMessage?: string;
};